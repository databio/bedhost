path:
  pipeline_output_path: $PIPELINE_OUTPUT_PATH
  bedstat_dir: $BEDSTAT_DIR
  bedbuncher_dir: $BEDBUNCHER_DIR
  remote_url_base: $REMOTE_URL_BASE
#  region2vec: "add/path/here"
#  vec2vec: "add/path/here"
database:
  host: $DB_HOST_URL
  port: $POSTGRES_PORT
  password: $POSTGRES_PASSWORD
  user: $POSTGRES_USER
  name: $POSTGRES_DB
  dialect: postgresql
  driver: psycopg2
qdrant:
  host: $QDRANT_HOST
  port: $QDRANT_PORT
  api_key: $QDRANT_API_KEY
  collection: $QDRANT_API_KEY

server:
  host: $SERVER_HOST
  port: $SERVER_PORT
  
remotes:
  http:
    prefix: /home/bnt4me/virginia/repos/bedbase_all/bedbase_autouploader/bbuploader/outputs/outputs
    description: HTTP compatible path
  s3:
    prefix: s3://data2.bedbase.org/
    description: S3 compatible path
schema_path: "/home/bnt4me/virginia/repos/bbconf/bbconf/schemas/bedfiles_schema.yaml"